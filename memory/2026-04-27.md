# 2026-04-27

## Environment & Bootstrap (Sat 25 → Sun 26)

- **Identity:** ClawPM 🦞 — enterprise-grade product & engineering execution agent for Linda
- **User:** Linda, Asia/Dubai GMT+4
- **Runtime:** OpenClaw `2026.4.23` → `2026.4.24`
- **Bearer model:** Switched from `anthropic/claude-opus-4-7` to `deepseek/deepseek-v4-flash` (Linda manually edited openclaw.json). Both Anthropic + DeepSeek providers configured with valid API keys.
- **Anthropic key:** Was showing HTTP 401 (stale billing probe, not actual inference failure). Verified working via curl.
- **Telegram bot** `@vibai223_bot` set up. Repeated spam from user `Orange123` (6515805673, @lebert1314) — told them once it's private, then stopped responding (NO_REPLY).

## Gateway & Networking

- **Gateway** port 18789 — required multiple restarts after config changes.
- **Conda** installed via Homebrew at `/opt/homebrew/Caskroom/miniconda/base/bin/conda`
- **Environment `deepseek-ocr`** created (Python 3.12.9, all pip deps installed)
- **Tesseract OCR** installed + 163 language packs
- **Bonjour mDNS** plugin disabled to fix WebSocket disconnect loop

## DeepSeek-OCR / OCR-2 on Mac MPS (Research Grade)

- DeepSeek-OCR (v1) cloned and runnable on Mac Apple Silicon via MPS + CPU fallback
- DeepSeek-OCR-2 (v2) also exists at `~/Desktop/bintu66/bintu66/DeepSeek-OCR-2/`
- Both versions run the **image encoding** pipeline correctly on MPS (BASE + PATCHES tensors, bounding box visualization)
- **Text generation fails** on MPS: only outputs token ID `[1]` (special/control token, then stops). Root cause: model uses bfloat16 (CUDA-native) which loses precision on MPS, producing garbage logits. float32 conversion fails due to mixed-dtype conv layers in the SAM vision encoder submodule.
- **Conclusion:** DeepSeek-OCR/2 is research-grade on Mac. Pipeline works visually but text output requires CUDA for precision. Not production-viable on Apple Silicon.

## Skills Installed (via npx skills add)

| Skill | Source | Status |
|---|---|---|
| find-skills | vercel-labs | ✅ ready |
| project-manager | mckruz/comfyui-expert (ComfyUI) | ✅ ready |
| ai-video-generation | infsh-skills | ✅ ready |
| claude-api | anthropics | ✅ ready |

## OpenClaw Built-in Skills Configured

- **github** — ✅ `gh` CLI installed
- **video-frames** — ✅ `ffmpeg` installed
- **summarize** — ✅ installed
- **notion** — ⚠️ key configured (`ntn_6015...V5gs`) but not verified working

## Memory & Help Reorganization (2026-04-27)

- **MEMORY.md** — 创建，按领域分类整理长期记忆（身份/配置/Skills/OCR/Channels/沟通偏好）
- **docs/help-reference.md** — 创建，内置 55 Skills + CLI 命令按用途分类
- `find-skills` 在 `.agents/` `.claude/` `.trae/` 等多处副本 → 已清理
- 原始日志保留在 `memory/YYYY-MM-DD.md` 不做改动

## Notes
- Linda operates in Chinese when comfortable, switches to English for technical terms
- Prefers: direct, structured output; PRD/SOP-ready; tables for comparisons
- Always separate: product / interaction / data / engineering logic
- Always surface: risks, missing fields, edge cases, next actions
- Safety: confirm before delete, overwrite config, destructive commands, secret exposure
